# Common Components Directory

This directory contains design-system level, atomic UI components that are stateless or local-state and highly reusable across the application (e.g. Buttons, Cards, Modals, Inputs).

## Guidelines:
- Place styling here that matches standard theme definitions.
- Keep them independent of business/domain logic.
